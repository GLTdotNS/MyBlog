"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.prefixCommand = void 0;
const isNpx = process.argv.some(segment => segment.includes('_npx')) || process.env.npm_lifecycle_event === 'npx';

const prefixCommand = (command = '') => (isNpx ? `npx @sanity/cli ${command}` : `sanity ${command}`).trim();

exports.prefixCommand = prefixCommand;